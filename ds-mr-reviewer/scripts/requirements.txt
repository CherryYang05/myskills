# MR Reviewer Skill - Python依赖列表
#
# 安装方式（使用pip）：
#   pip install -r scripts/requirements.txt
#
# 安装方式（使用uv）：
#   uv pip install -r scripts/requirements.txt --system
#
# 或逐个安装：
#   uv pip install requests requests-toolbelt pyyaml httpx fastmcp pywin32 --system

# ============================================================
# 核心依赖 - MR检视基本功能必需
# ============================================================

requests>=2.31.0
requests-toolbelt>=0.10.1
urllib3>=2.0.0
pyyaml>=6.0

# ============================================================
# 可选依赖 - WeLink通知功能必需（Windows环境）
# ============================================================

httpx>=0.27.0
fastmcp>=2.0.0
pywin32>=306; platform_system == "Windows"
