---
name: tech-blog-writer
description: 在技术对话结束后，将整个聊天内容整理成一篇结构完整的中文技术博客文章。当用户说"帮我整理成博客"、"生成博客"、"写成文章"、"整理一下"、"出一篇博客"，或者明确说"教程类博客"/"技术解析博客"时，必须立即触发本技能。即使用户只是随口说"总结一下聊天内容"，也应该主动询问是否需要生成博客。博客以中文撰写，技术术语保留英文原文。
---

# Tech Blog Writer

将技术对话整理成发布级博客文章的技能。

---

## 第一步：确认博客类型

如果用户**明确指定**了类型（"教程"/"技术解析"），直接跳到第二步。

如果用户**未指定**，使用 `ask_user_input_v0` 工具弹出选择框询问：

```
问题：这篇博客是哪种类型？
选项：
  - 教程 / How-to（工具使用、环境配置、操作步骤）
  - 技术深度解析（原理分析、源码阅读、设计决策）
```

---

## 第二步：梳理对话内容

通读整个对话，提取以下要素：
- **核心问题/主题**：这次对话在解决什么问题？
- **关键知识点**：涉及哪些概念、原理、工具？
- **过程脉络**：问题是如何被分析和解决的？
- **结论与收获**：最终达成了什么？有哪些值得记录的结论？

**注意**：博客是整理提炼，而非聊天记录的逐字转写。去除来回试错的冗余过程，保留有价值的信息。代码、命令、配置片段需要完整保留。

---

## 第三步：按类型生成博客

### 类型 A：教程 / How-to

**固定骨架：**

```
# [标题：动词开头，说明做什么，如"在 WSL2 中配置 Docker 代理"]

> **核心目的**：一句话点明做这件事能解决什么问题。
> 讲人话：[用更口语的方式再说一遍，降低理解门槛]

## 背景与目标          ← 必须有
[为什么需要做这件事？不做会有什么问题？完成后达到什么效果]

## 环境与前提          ← 按需，若无依赖可省略
[操作系统版本、依赖工具、前置条件]

## [核心步骤章节]      ← AI 自由划分，通常 2-5 个步骤章节

每个步骤的内部节奏：
  1. 先给完整命令/配置代码块
  2. 解释关键行"是如何工作的"（不是每行都解释，只解释不直觉的地方）
  3. 给出预期输出（如果有）

## 常见问题            ← 按需，若对话中有踩坑记录则必须包含
[用"**遇到 XXX 报错怎么办？**"格式设悬念，然后给出解决方案]

## 总结                ← 必须有
[一句话精华：这个方案的核心价值是什么]
```

**写作规范：**
- 开头 blockquote 必须有"讲人话"的通俗解释，让读者秒懂这篇文章解决什么问题
- 命令/配置用代码块，注意事项用 `> ⚠️` blockquote，两种格式不混用
- 语气：直接，面向已有一定基础的工程师，不写"接下来我们..."这类废话
- 每个步骤结束后展示预期输出，读者可以对照验证

---

### 类型 B：技术深度解析

**固定骨架：**

```
# [标题：名词短语，说明分析对象，如"CCDB Paxos 日志删除机制解析"]

> **核心结论**：用一句话点明这个机制/技术的本质是什么。
> 讲人话：[用更口语的方式再说一遍]

## 问题背景            ← 必须有
[这个技术点来自哪个系统/场景？]
[展示"没有这个机制/没用这个技术"时会遇到的具体问题，最好配代码或现象]
[不解决它会出什么错——让读者感受到"痛点"]

## [核心机制/实现]     ← AI 自由划分，通常 2-4 个章节

每个章节的内部节奏（参考 X-Macro 文章的 Step 结构）：
  1. 给出核心代码/设计/结构
  2. 用"**XXX 是如何工作的？**"设悬念
  3. 逐步拆解执行过程（可以用 1/2/3 有序步骤）
  4. 给出"最终展开/执行结果"，形成前后对比

可选章节（按需）：
  - 关键细节 / 边界情况
  - 与同类方案的对比（如"为什么不用 XXX？"）
  - 踩坑记录

## 总结                ← 必须有

用"普通写法 vs 本文方案"的对比格式，强化"为什么要这样做"：

**普通写法：**
1. [描述普通写法的第一步]
2. [描述普通写法的典型问题——漏掉某步、需要重复维护等]
3. 结果：[出什么错，或者维护成本高在哪]

**[本文方案]写法：**
1. [只需要做什么]
2. [其他的自动搞定]
3. 结果：[绝对不会出现 XXX 问题]

最后一行：**[核心概念]**：一句话提炼本文技术的本质思想。
```

**写作规范：**
- 开头 blockquote 的"讲人话"是关键：把晦涩的技术用一句大白话解释清楚
- 问题背景必须展示"痛点代码"——让读者看到不用这个技术时有多麻烦
- 每个章节分析时，先展示代码，再用"**XX 是如何工作的？**"引出解释，最后展示"最终生成/展开的结果"
- 总结部分强制使用"普通写法 vs 新方案"对比，强化读者对方案价值的认知
- 技术术语保留英文（Paxos、WAL、LWT、epoch），但首次出现时可以用括号补充一句中文说明

---

## 第四步：交付

### 在对话中预览

直接在对话里输出完整 Markdown 内容，让用户可以即时阅读和检查。

### 生成可下载文件

使用 `create_file` 工具将博客保存为 `.md` 文件，文件名格式：

```
YYYY-MM-DD-[slug].md
```

其中 slug 为标题的小写英文缩写（用连字符连接），例如：

```
2026-04-05-wsl2-docker-proxy-setup.md
2026-04-05-ccdb-paxos-log-deletion.md
```

保存到 `/mnt/user-data/outputs/`，然后用 `present_files` 工具提供下载链接。

---

## 质量检查清单

生成后自查：
- [ ] 标题是否准确描述了内容？
- [ ] 背景章节是否说清楚了"为什么"？
- [ ] 代码块是否完整、语法高亮标注正确？
- [ ] 对话中的报错/坑是否被记录在文章中？
- [ ] 总结是否提炼了真正有价值的结论（非复读正文）？
- [ ] 技术术语英文是否保留？中文表达是否自然？

---

## 🔁 自迭代评估标准

> 由 skill-iterate 元 skill 读取。本 section 不影响博客生成流程。
> 描述本 skill 输出的质量评估维度，供人工标注时对照使用。

```yaml
sample_description: |
  一段技术对话（input）+ 对应生成的博客 Markdown（output）。
  教程类和技术解析类各需要至少一个样本。

rubric:
  structure_completeness:
    weight: 0.25
    pass_threshold: 7.0
    criteria: |
      根据博客类型检查所有「必须有」的章节是否存在且内容完整。
      教程必须有：背景与目标、至少一个核心步骤章节、总结。
      技术解析必须有：问题背景（含痛点）、核心机制章节、总结（含普通 vs 新方案对比）。
      缺少必须章节每个扣 2 分；章节存在但内容空洞（如只有标题没有实质内容）每个扣 1 分。

  opening_blockquote:
    weight: 0.20
    pass_threshold: 7.0
    criteria: |
      开头 blockquote 必须有两层：①核心结论/目的；②「讲人话」（口语零门槛）。
      「讲人话」合格标准：初中生能听懂，不出现任何技术术语。
      缺少「讲人话」层扣 4 分；「讲人话」仍含技术术语扣 2 分；无 blockquote 扣满（0 分）。

  formatting_discipline:
    weight: 0.20
    pass_threshold: 7.0
    criteria: |
      命令/配置必须用代码块（```）；注意事项/警告必须用 `> ⚠️` blockquote。
      两种格式不混用：注意事项不进代码块，命令不进 blockquote。
      每处格式错误扣 1.5 分。

  information_density:
    weight: 0.20
    pass_threshold: 6.5
    criteria: |
      博客是提炼，不是对话复述。三项检查：
      ① 去掉了来回试错的冗余交互（明显复读扣 3 分）
      ② 关键代码/命令/配置完整保留（丢失一处扣 2 分）
      ③ 对话中出现的报错和解决方案被记录（漏掉一处扣 1.5 分）

  hook_pattern:
    weight: 0.15
    pass_threshold: 6.0
    criteria: |
      仅技术解析类博客检查此维度，教程类直接给 10 分。
      核心机制章节内部必须使用「XXX 是如何工作的？」引出拆解，
      最终给出执行结果/展开结果形成前后对比。
      应该用但未用此模式的章节每个扣 2 分。

convergence_threshold: 8.0
max_rounds: 5
```
